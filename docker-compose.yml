version: '3.8'

services:
  nanobot:
    image: smanx/nanobot:latest
    container_name: nanobot
    restart: unless-stopped
    volumes:
      - nanobot_data:/root/.nanobot
    environment:
      # LLM Provider API Keys (set in Portainer stack env vars)
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - ZAI_API_KEY=${ZAI_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      # Provider/model selection
      - NANOBOT_PROVIDER=${NANOBOT_PROVIDER:-zhipu}
      - NANOBOT_MODEL=${NANOBOT_MODEL:-glm-5}
      - TZ=UTC
    ports:
      - "8765:8765"
    # Generate config.json from env vars on startup
    entrypoint:
      - /bin/sh
      - -c
      - |
        mkdir -p /root/.nanobot
        cat > /root/.nanobot/config.json << 'CONFIGEOF'
        {
          "providers": {
            "openrouter": {
              "apiKey": "$$OPENROUTER_API_KEY"
            },
            "zhipu": {
              "apiKey": "$$ZAI_API_KEY"
            },
            "openai": {
              "apiKey": "$$OPENAI_API_KEY"
            },
            "anthropic": {
              "apiKey": "$$ANTHROPIC_API_KEY"
            },
            "deepseek": {
              "apiKey": "$$DEEPSEEK_API_KEY"
            },
            "gemini": {
              "apiKey": "$$GEMINI_API_KEY"
            }
          },
          "agents": {
            "defaults": {
              "model": "$$NANOBOT_MODEL",
              "provider": "$$NANOBOT_PROVIDER"
            }
          }
        }
        CONFIGEOF
        exec nanobot gateway
    stdin_open: true
    tty: true

volumes:
  nanobot_data:
    driver: local
